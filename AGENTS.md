# Project context

Read [CONTEXT.md](CONTEXT.md) before planning or implementing product changes. It records the agreed direction, current milestone, architectural rules, and links to the complete specification.

The user-authored plans live in `docs/plan/`. Later amendments supersede conflicting details in earlier documents; consult the precedence order in `CONTEXT.md`.

Keep work within the requested milestone. Distinguish planned capabilities from implemented behavior in documentation. Update `CONTEXT.md` when implementation progress or explicit user decisions materially change the recorded state.
